# Core Dependencies for Hindi Medical Audio ASR Model Comparison Framework
# Python 3.8+ required

# Deep Learning Framework
torch>=2.0.0
torchaudio>=2.0.0

# Transformers and Models
transformers>=4.30.0

# Audio Processing
soundfile>=0.12.0
numpy>=1.24.0

# Evaluation Metrics
jiwer>=3.0.0

# Configuration
pyyaml>=6.0

# Reporting
tabulate>=0.9.0

# Optional: API models
openai>=1.0.0
google-generativeai>=0.3.0

# Optional: Environment management
python-dotenv>=1.0.0

# Optional: Better transliteration for Devanagari <-> Roman
indic-transliteration>=2.3.0

# Optional: Security (for torch < 2.6)
safetensors>=0.4.0

torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.37.0
jiwer>=3.0.0
pyyaml>=6.0
numpy>=1.24.0
soundfile>=0.12.1
librosa>=0.10.0

# For Qwen-Audio model
tiktoken>=0.5.0
matplotlib>=3.7.0
transformers_stream_generator>=0.0.4
einops>=0.7.0

# Optional but recommended
accelerate>=0.20.0

# Development Dependencies (Optional)
# pytest>=7.4.0
# pytest-asyncio>=0.21.0

# Install PyTorch with CUDA 11.8
# need to run this manually for gpu on laptops
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118

# for nvidiea gpu support
# Uninstall current PyTorch
#pip uninstall torch torchaudio -y

# Install with CUDA 12.1 (compatible with CUDA 13.0)
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121